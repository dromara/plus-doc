# 不支持 ST 请求
- - -
## 问题原因
**经多人反馈 共同点为全都是做`小程序开发`使用的`uniapp`发送的网络请求而出现这种问题**

在小程序或 `uniapp` 场景中错误设置了 `Content-Type`，导致非 JSON 请求也被当作 JSON 处理。

## 解决方案

1. 升级到已兼容该问题的框架版本。
2. 在 `uniapp` 中为不同请求类型设置正确的 `Content-Type`。
